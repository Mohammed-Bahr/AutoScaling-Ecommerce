apiVersion: v2
name: master-ecommerce
description: A master umbrella chart for the entire E-commerce stack
type: application
version: 1.0.0
appVersion: "1.0.0"

# --- ADD THIS DEPENDENCIES BLOCK ---
#dependencies:
  #  - name: my-frontend
    #    version: 0.1.0          # Must match the 'version' inside your frontend's Chart.yaml
      #    repository: "file://../charts/my-frontend"
      #  - name: my-backend
    #    version: 0.1.0          # Must match the 'version' inside your backend's Chart.yaml
      #    repository: "file://../charts/my-backend"
      #  - name: my-Db
    #    version: 0.1.0          # Must match the 'version' inside your Db's Chart.yaml
      #    repository: "file://../charts/my-Db"
