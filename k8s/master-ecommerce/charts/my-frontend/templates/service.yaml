apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-frontend.fullname" . }}
  labels:
    app: {{ include "my-frontend.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "my-frontend.name" . }}
