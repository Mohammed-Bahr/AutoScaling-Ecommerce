apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: ecom
type: Opaque
stringData:
  # Override per environment. Never commit real secrets in prod.
  JWT_SECRET: "abac12afsdkjladf"
  PAYPAL_CLIENT_ID: ""
  # Connection string uses the in-cluster Mongo service. The app user is created
  # by mongo-init with readWrite on the depi-project database.
  MONGO_URI: "mongodb://appuser:change-me-app@my-mongo:27017/depi-project?authSource=depi-project"
